# AOSystem

Container for the objects that make up an AO system — telescope, atmosphere,
deformable mirror, guide stars and subaperture masks. An `AOSystem` instance is
the single argument required to build a
[`tomoReconstructor`](reconstructor.md).

::: tomoAO.Simulation.aoSystem.AOSystem
